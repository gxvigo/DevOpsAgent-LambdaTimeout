name: Deploy CloudFormation to Account One

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_ACC_ONE }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_ACC_ONE }}
          aws-region: ap-southeast-2
      
      - name: Deploy CloudFormation stack
        run: |
          aws cloudformation deploy \
            --template-file acc-one-template.yaml \
            --stack-name quote-of-day-acc-one \
            --parameter-overrides AccountTwoId=639930233929 \
            --capabilities CAPABILITY_IAM \
            --region ap-southeast-2